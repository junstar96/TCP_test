import stageModels from "../models/stage.models";
import { addUser,getUser,removeUser,userRank } from "../models/user.models";

//필요 기능
//1. 아이템의 테이블을 변경한다.
export const UpdateItemList = (uuid) => {
    //해당 유저의 데이터를 읽고 그 플레이어의 스테이지에 따라서 아이템 테이블을 변경한다.

}


//2. 스테이지의 설정을 변경한다.